/**
 * EnrollmentCheck — simplified version.
 * In the new standalone backend, enrollment happens at account creation (invite acceptance).
 * This component simply renders its children.
 */
export default function EnrollmentCheck({ children }) {
  return children;
}
